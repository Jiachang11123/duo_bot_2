name: 24/7 Unlimited Nuclear

on:
  schedule:
    - cron: "0 */6 * * *" # 每 6 小時強制重啟一次
  workflow_dispatch:      # 允許手動按按鈕開始

jobs:
  nuclear-attack:
    runs-on: ubuntu-latest
    # 這裡已經不需要 matrix 了，單一任務最穩
    permissions:
      contents: read
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Restore VPN Configs
        run: |
          echo "${{ secrets.VPN_ZIP }}" | base64 -d > vpn_configs.zip
          unzip vpn_configs.zip -d .
          ls -R vpn_configs

      - name: Install OpenVPN & Python
        run: |
          sudo apt-get update
          sudo apt-get install -y openvpn
          pip install -r requirements.txt || true
          pip install curl_cffi colorama

      - name: Run Nuclear Bot
        env:
          BOT_ID: "1" # 🟢 修正：直接指定編號 1，不要用 matrix
          DUO_TOKEN: ${{ secrets.DUO_TOKEN }}
          VPN_USER: ${{ secrets.VPN_USER }}
          VPN_PASS: ${{ secrets.VPN_PASS }}
          LINE_ACCESS_TOKEN: ${{ secrets.LINE_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          # 👇 這是神風模式的核心：無限復活
          # 當 Python 自殺退出時，這裡會馬上接手，休息 2 秒後立刻重啟
          timeout 350m bash -c "while true; do python main-auto-vpn.py; echo '♻️ 墜機重啟中...'; sleep 2; done" || true
