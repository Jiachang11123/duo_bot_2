name: 24/7 Unlimited Nuclear

on:
  schedule:
    - cron: "0 */6 * * *" # 每 6 小時強制重啟一次，保持環境新鮮
  workflow_dispatch:      # 允許手動按按鈕開始

jobs:
  nuclear-attack:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Restore VPN Configs
        run: |
          echo "${{ secrets.VPN_ZIP }}" | base64 -d > vpn_configs.zip
          unzip vpn_configs.zip -d .

      - name: Install OpenVPN & Python
        run: |
          sudo apt-get update
          sudo apt-get install -y openvpn
          pip install -r requirements.txt || true
          pip install curl_cffi colorama

      - name: Run Nuclear Bot
        env:
          # 這裡不需要 matrix，單一帳號跑單一任務最快
          DUO_TOKEN: ${{ secrets.DUO_TOKEN }}
          VPN_USER: ${{ secrets.VPN_USER }}
          VPN_PASS: ${{ secrets.VPN_PASS }}
          LINE_ACCESS_TOKEN: ${{ secrets.LINE_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          # 👇 這才是重點：無限復活迴圈
          # 當 Python 遇到阻擋自殺後，這裡會接手：
          # 1. 顯示休息訊息
          # 2. 等待 15 秒 (讓 IP 冷卻)
          # 3. 重新執行 Python (Python 會自動換新 VPN)
          timeout 350m bash -c "while true; do python main-auto-vpn.py; echo '♻️ 觸發重啟機制，冷卻 15 秒...'; sleep 15; done" || true
