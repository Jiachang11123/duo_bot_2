name: 24/7 Unlimited Nuclear

on:
  schedule:
    - cron: "0 */6 * * *" # 每 6 小時強制換一台新電腦
  workflow_dispatch:      # 手動開關

jobs:
  nuclear-attack:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Restore VPN Configs
        run: |
          echo "${{ secrets.VPN_ZIP }}" | base64 -d > vpn_configs.zip
          unzip vpn_configs.zip -d .

      - name: Install OpenVPN & Python
        run: |
          sudo apt-get update
          sudo apt-get install -y openvpn
          pip install -r requirements.txt || true
          pip install curl_cffi colorama

      - name: Run Nuclear Bot
        env:
          # ✅ 這裡改好了！直接指定 "1"，一定要這樣寫程式才不會報錯
          BOT_ID: "1"
          DUO_TOKEN: ${{ secrets.DUO_TOKEN }}
          VPN_USER: ${{ secrets.VPN_USER }}
          VPN_PASS: ${{ secrets.VPN_PASS }}
          LINE_ACCESS_TOKEN: ${{ secrets.LINE_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          # 👇 這就是你要的：無限復活 + 自動換 IP 機制
          # 只要 Python 遇到問題自殺，這裡就會馬上重啟它 (且 Python 啟動時會自己換 VPN)
          timeout 350m bash -c "while true; do python main-auto-vpn.py; echo '♻️ 被鎖重啟，換個 IP 再戰...'; sleep 2; done" || true
